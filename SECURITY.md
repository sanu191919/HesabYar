# مدل امنیتی حساب‌یار

## داده‌هایی که ذخیره می‌شوند

برای تراکنش پیامکی فقط مبلغ، نوع تراکنش، نام بانک تشخیص‌داده‌شده، چهار رقم انتهایی حساب در صورت وجود، زمان و امتیاز اطمینان ذخیره می‌شود. متن کامل پیامک و شماره کامل کارت یا حساب ذخیره نمی‌شود.

## رمزگذاری

فیلدهای مالی با AES-256-GCM و IV تصادفی برای هر مقدار رمزگذاری می‌شوند. کلید اصلی قابل خروج از Android Keystore نیست. پایگاه داده در فضای خصوصی برنامه قرار دارد و نسخه پشتیبان سیستم‌عامل برای آن غیرفعال شده است.

## سطح دسترسی

تنها مجوزهای حساس برنامه `READ_SMS`، `RECEIVE_SMS` و `USE_BIOMETRIC` هستند. مجوز `INTERNET` عمداً در Manifest وجود ندارد.

## محدودیت‌ها

- دستگاه روت‌شده یا سیستم‌عامل دستکاری‌شده می‌تواند مدل امنیتی اندروید را تضعیف کند.
- اگر قفل صفحه گوشی حذف، کلید سخت‌افزاری باطل یا برنامه پاک شود، بازیابی داده ممکن نیست.
- تشخیص پیامک مبتنی بر قواعد محلی است؛ به همین دلیل همه نتایج ابتدا وارد صف تأیید می‌شوند.
- این نرم‌افزار جایگزین صورتحساب رسمی بانک نیست.
